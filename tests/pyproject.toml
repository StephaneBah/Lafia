[project]
name = "lafia-tests"
version = "0.1.0"
description = "Suite boîte noire : la pile en marche, jointe par la passerelle seule."
requires-python = ">=3.11"
dependencies = [
    "httpx>=0.28,<0.29",
    "pytest>=9,<10",
]

[tool.uv]
package = false

[tool.pytest.ini_options]
addopts = "-ra"
