# SoonCard

Un service à venir, section *Demain* du site : surface pervenche, badge « Bientôt » en pointillés, aucun bouton.

**À fournir :** `title`, `tagline` (une phrase), `children` (détail), `icon` (nom Phosphor duotone).

- Volontairement plus léger qu'une ServiceCard : ce n'est pas encore une porte.
